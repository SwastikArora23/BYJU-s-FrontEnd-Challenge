1st---
open terminal and install angular "npm install -g @angular/cli"


2nd---
place this folder in D: drive

go to new terminal

the current working directory should be "D:/Swastik"

run command "npm install --save"

it should take a while

when done.

"cls"

"ng serve"

then head to chrome and open "localhost:4200"

it should work
